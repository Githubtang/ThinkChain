# ThinkChain 项目协作规则

本文件适用于整个仓库。执行 Java/Spring 后端的编码、修改、重构或 Review 时，还必须加载 `.codex/skills/thinkchain-backend-style/SKILL.md`。

## 始终遵守

- 修改前阅读目标文件、同包相邻实现和已有同类能力，优先复用现有组件与调用链。
- 采用完成任务所需的最小改动；不顺带重构无关代码，不过度封装，不为了设计模式而引入设计模式。
- 不得为了测试在正式生产代码中增加特殊构造器、方法、分支、开关或降低可见性；测试必须适配正式代码。
- Spring Bean 使用唯一构造器注入时不要显式添加 `@Autowired`。
- Controller 不得信任或透传客户端提交的用户归属、审计字段、统计字段、处理状态、存储路径等服务端维护字段；身份必须来自服务端认证上下文，资源操作必须校验归属和权限。
- 核心业务、状态转换、安全边界、兼容性原因等非显然逻辑添加有价值的中文注释；不要逐行复述代码，也不要保留大段失效的注释代码。
- 不输出、记录或提交密钥、Token、完整敏感请求体和其他凭据。
- 修改后必须运行与改动范围匹配的测试和 Maven 构建；若受环境限制无法完成，明确报告未验证项和原因，不得声称已通过。
- 保留用户已有修改，不修改与当前任务无关的业务代码、配置和数据。


<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyh.chat.conversation.mapper.ChatMessageMapper">

    <resultMap type="ChatMessage" id="ChatMessageResult">
        <id     property="id"             column="id"              />
        <result property="conversationId" column="conversation_id" />
        <result property="role"           column="role"            />
        <result property="contentType"    column="content_type"    />
        <result property="content"        column="content"         />
        <result property="rawContent"     column="raw_content"     />
        <result property="model"          column="model"           />
        <result property="createTime"     column="create_time"     />
    </resultMap>

    <sql id="selectChatMessageVo">
        select id, conversation_id, role, content_type, content, raw_content, model, create_time
        from chat_message
    </sql>

    <select id="selectChatMessageById" parameterType="String" resultMap="ChatMessageResult">
        <include refid="selectChatMessageVo"/>
        where id = #{id}
    </select>

    <select id="selectChatMessageList" parameterType="ChatMessage" resultMap="ChatMessageResult">
        <include refid="selectChatMessageVo"/>
        <where>
            <if test="conversationId != null and conversationId != ''">
                and conversation_id = #{conversationId}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
        </where>
        order by create_time asc
    </select>

    <insert id="insertChatMessage" parameterType="ChatMessage">
        insert into chat_message (
            id, conversation_id, role, content_type, content, raw_content, model, create_time
        ) values (
            #{id}, #{conversationId}, #{role}, #{contentType}, #{content}, #{rawContent}, #{model}, sysdate()
        )
    </insert>

    <delete id="deleteChatMessagesByConversationId" parameterType="String">
        delete from chat_message where conversation_id = #{conversationId}
    </delete>

</mapper>

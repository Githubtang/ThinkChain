<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyh.chat.rag.knowledge.mapper.KnowledgeBaseMapper">

    <resultMap type="KnowledgeBase" id="KnowledgeBaseResult">
        <id     property="id"            column="id"             />
        <result property="userId"        column="user_id"        />
        <result property="name"          column="name"           />
        <result property="description"   column="description"    />
        <result property="status"        column="status"         />
        <result property="documentCount" column="document_count" />
        <result property="chunkCount"    column="chunk_count"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectKnowledgeBaseVo">
        select id, user_id, name, description, status, document_count, chunk_count, create_time, update_time
        from knowledge_base
    </sql>

    <select id="selectKnowledgeBaseById" parameterType="String" resultMap="KnowledgeBaseResult">
        <include refid="selectKnowledgeBaseVo"/>
        where id = #{id}
    </select>

    <select id="selectKnowledgeBaseList" parameterType="KnowledgeBase" resultMap="KnowledgeBaseResult">
        <include refid="selectKnowledgeBaseVo"/>
        <where>
            <if test="userId != null and userId != ''">and user_id = #{userId}</if>
            <if test="name != null and name != ''">and name like concat('%', #{name}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by update_time desc, create_time desc
    </select>

    <insert id="insertKnowledgeBase" parameterType="KnowledgeBase">
        insert into knowledge_base (
            id, user_id, name, description, status, document_count, chunk_count, create_time, update_time
        ) values (
            #{id}, #{userId}, #{name}, #{description}, #{status}, #{documentCount}, #{chunkCount}, sysdate(), sysdate()
        )
    </insert>

    <update id="updateKnowledgeBase" parameterType="KnowledgeBase">
        update knowledge_base
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="documentCount != null">document_count = #{documentCount},</if>
            <if test="chunkCount != null">chunk_count = #{chunkCount},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteKnowledgeBaseById" parameterType="String">
        delete from knowledge_base where id = #{id}
    </delete>

</mapper>
